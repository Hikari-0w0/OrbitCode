# Web 流式对话 Checklist

状态：已批准
依据：已批准的 `spec.md`、`plan.md` 与 `task.md`

> 每项只记录实际执行证据；验证保持精简，以关键成功路径、取消/恢复、安全边界和项目检查为主。

## 需求验收

- [x] C1 / AC1、AC3：首页包含完整聊天工作区；空白不可发送，Enter 提交、Shift+Enter 换行，桌面和移动布局无横向溢出。（验证：浏览器操作；证据：见实际结果）
- [x] C2 / AC2：单 Provider 自动选中；配置接口只返回安全元数据，配置不可用时禁止发送并显示安全错误。（验证：协议测试与浏览器观察；证据：见实际结果）
- [x] C3 / AC4、AC5：浏览器在完成前显示首段增量，两轮请求保持成功历史且 CLI 行为不回退。（验证：自动化测试与真实两轮浏览器对话；证据：见实际结果）
- [x] C4 / AC6：生成期间停止后显示取消状态并恢复输入，下一轮不携带取消轮次。（验证：浏览器冒烟与协议测试；证据：见实际结果）
- [x] C5 / AC7、AC10：非法请求和代表性流失败返回脱敏错误，页面保持可恢复，凭据与上游正文不出现在响应或 UI。（验证：协议测试与代码边界检查；证据：见实际结果）
- [x] C6 / AC8：成功对话后清空恢复空状态，后续历史从零开始；生成期间清空禁用。（验证：浏览器操作；证据：见实际结果）
- [x] C7 / AC9：底部时流式跟随，向上浏览时显示回到底部操作且不强制跳转。（验证：滚动状态实现检查与长回复浏览器观察；证据：见实际结果）
- [x] C8 / AC11、AC12：结束、停止和失败后无残留请求；模型 HTML 样式内容仅按纯文本显示，依赖清单无新增包。（验证：测试、React 文本节点检查、`npm ls --depth=0`；证据：见实际结果）

## 集成与架构

- [x] C9：`src/core/` 与 `src/models/provider.ts` 不依赖 React、Next.js 页面或 Web 组件；CLI 与 Web 共用核心与 Provider。（验证：依赖扫描、现有测试；证据：见实际结果）
- [x] C10：Web API 只在服务端读取固定 `.env`/YAML，浏览器包不包含 Node 文件读取、base URL 或 API Key。（验证：构建与源码检查；证据：见实际结果）
- [x] C11：请求断开、停止、成功和失败都清理 AbortSignal 与流读取资源。（验证：相关测试与浏览器停止操作；证据：见实际结果）

## 安全与异常路径

- [x] C12：未知字段、非法角色/顺序、空内容、超长消息、过多历史、过大请求体和未知 Provider 在上游前被拒绝。（验证：协议与既有配置测试；证据：见实际结果）
- [x] C13：DOM、API 响应、控制台和测试输出不包含凭据、Authorization、完整 YAML、上游正文或堆栈。（验证：哨兵测试与浏览器检查；证据：见实际结果）
- [x] C14：README 明确 Web 无认证、仅限本机或可信网络，且 `.env`/`orbitcode.yaml` 继续保持未跟踪。（验证：文档与 `git ls-files`；证据：见实际结果）

## 项目检查

- [x] C15：全部自动化测试通过。（验证：`npm run test`，退出码 `0`；证据：49/49）
- [x] C16：ESLint 通过。（验证：`npm run lint`，退出码 `0`）
- [x] C17：TypeScript 严格类型检查通过。（验证：`npm run typecheck`，退出码 `0`）
- [x] C18：Next.js 生产构建通过。（验证：`npm run build`，退出码 `0`）
- [x] C19：补丁无空白错误，未跟踪私密配置或测试产物。（验证：`git diff --check`、`git status --short`、`git ls-files`；证据：见实际结果）

## 端到端

- [x] C20 / AC13：开发服务器下使用真实浏览器完成至少两轮真实模型流式对话，第二轮体现上下文，页面无错误覆盖层且控制台无错误。（验证：浏览器；证据：见实际结果）
- [x] C21 / AC13：在浏览器完成一次停止和清空，并检查 390×844 移动视口；结束后关闭开发服务器，无残留验证进程。（验证：浏览器与进程状态；证据：见实际结果）

## 验收报告

结果：21/21，满足已批准的 Web 流式纯对话验收标准。

### 已通过

- [x] 自动化：`npm run test` 退出码 `0`，49/49；新增配置列举、无状态 Web 历史、严格请求与 Web SSE 契约测试，既有 CLI/Provider/SSE 测试全部保留通过。
- [x] 项目检查：`npm run lint`、`npm run typecheck`、`npm run build` 均退出码 `0`；构建产物包含静态首页及动态 `/api/providers`、`/api/chat`。
- [x] 安全：Provider 接口只返回 name/model/available；浏览器组件不读取 Node 配置；`git ls-files -- .env .env.local orbitcode.yaml '*.log'` 无输出；未新增依赖。
- [x] 浏览器：桌面首屏内容完整；一次视觉检查发现并修复低高度窗口输入区裁切；1280px 与 390px 视口均无横向溢出，错误覆盖层为 false，控制台错误为空。

### 端到端结果

- [x] 真实模型两轮：第一轮要求记住 `WEB-ORBIT-27` 并得到“已记住”，第二轮只问此前代号并正确得到 `WEB-ORBIT-27`。
- [x] 清空：两轮后点击清空，页面恢复空状态且清空按钮重新禁用。
- [x] 停止：提交长回复后停止按钮可用；点击后助手消息标记“已停止”，显示本轮不进入上下文，输入与 Provider 选择恢复可用。
- [x] 清理：浏览器会话已关闭，Next.js 开发服务器已通过 Ctrl-C 结束，端口 3000 无监听。

### 剩余风险

- Web API 无身份认证，仅适合本机或可信网络；README 已明确禁止直接暴露到不受信任公网。
- 当前历史仅保存在 React 页面内，刷新即丢失；这是本阶段明确范围。
