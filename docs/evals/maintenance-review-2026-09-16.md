# 调试评估与维护检查结果

日期：2026-09-16；当前代码 HEAD：`e41436f`，工作树包含本轮文档整理。范围：一条历史开发运行取证、现有维护边界检查、验收项目归类。没有改动运行时代码、调用真实模型或重跑用户项目。

更新：2026-09-17 已在用户授权下完成下面三项有限修复。原表保留问题发现时的事实，当前状态以本节为准。

| 项目 | 已处理 | 保留边界 |
| --- | --- | --- |
| R1 | 目标端口启动前已可连接时拒绝启动，返回可恢复、无副作用的冲突结果；补 IPv4/IPv6 反例和预先取消测试；结果标注端口观察范围 | TCP 探测不保证监听者归属，仍存在启动前检查后的竞争窗口；没有加入框架或固定端口特判 |
| R2 | 界面改为“所列检查通过”并展开检查范围；工具描述要求纳入未验证需求 | 不新增业务关键词判定；没有自动证明需求覆盖完整，不保证模型必然遵守说明 |
| R3 | 进程结果返回 current-turn 生命周期，工具和页面明确结束后回收 | 保持现有临时进程策略，没有新增后台预览托管；未回访历史运行的存活时间 |

验证：318/318 自动化测试通过，lint、typecheck、build 通过。agent-browser 在开发服务器上的临时测试页面验证了真实 MessageList 的范围展开和生命周期提示，页面无错误覆盖层、控制台错误或横向溢出；页面、浏览器和服务已清理。主页因现有本地 Workspace 配置包含不可访问目录而未完成实际会话验证，该配置未被修改；没有调用真实模型。

固定 System Prompt 保持原文，补充说明仅放在对应工具契约中，避免为了修复一个场景持续增加所有请求的固定上下文。R4–R9 和人工验收仍按证据与需求排序，不在本轮扩展。

## 流程实际验证

现有 CLI 单轮导出已成功走通，无需页面下载。样本是本地最近的已结束开发运行：22 次迭代、28 次工具调用、26 成功/2 失败、558098 ms、记录的累计 Token 292524。前后检查点均可读取，任务为三列看板开发。

完整导出与逐调用诊断保存在已忽略的 `tmp/agent-evals/`，文件名以 `fe9a6199-f920-486c-9bf8-ac60eba7afe7` 开头。不将完整对话和代码载荷纳入公开文档。摘要中的数字仅描述这一条历史样本，不代表当前模型效果或平均性能。

本次得出的流程修正：展示时间线中的工具参数可能已作安全摘要；要分析实际参数，应通过 callId 到 checkpoint.context.messages 的 assistant-tool-call 中取证。上下文也可能已被压缩，因此不能假设完整参数总是存在。

## 按优先级排列的处理清单

优先级表示后续处理顺序，不代表本轮已修复。

| ID | 优先级 / 类型 | 发现与证据 | 最小后续动作与通过标准 |
| --- | --- | --- | --- |
| R1 | P1 / 明确缺陷 | 历史运行要求等待 5173，但服务日志明确跳到 5174；[进程控制器](../../src/tools/managed-process.ts) 的 waitForPort 只判断 TCP 可连接，没有确认新进程拥有该端口 | 增加占用端口反例；明确冲突及就绪策略，无关服务不得使新进程 ready，保留 IPv4/IPv6 支持 |
| R2 | P1 / 验收缺口 | 历史 verified 的条目覆盖构建与启动，没有覆盖用户要求的增删、拖拽、刷新持久化；[完成检查](../../src/core/completion-tracker.ts) 检查所提交证据，不能自行保证需求齐全 | 固定需求验收列表；未执行交互时明确部分验证，不把模型自选清单当成完整任务标准 |
| R3 | P1 / 契约对齐 | 历史最终文本提供持续预览的表述；当前 [聊天路由](../../src/app/api/chat/route.ts) 的 onFinished 回收本轮进程 | 对齐“本轮临时验证”和“回复后继续预览”的产品语义；不在整理阶段擅自改成长驻后台服务 |
| R4 | P2 / 证据缺口 | [运行跟踪](../../src/web/chat-handler.ts) 内部有 callId，落盘摘要不含此键；模型阶段只保留最后值，版本信息不完整 | 后续先补关联键和实验身份，按真实诊断需要补阶段边界；验收同名多调用可准确关联 |
| R5 | P2 / 可观测性缺口 | chat-handler 的日志写入异常被吞掉，日志仅在 finish 写入 | 保留聊天可用性，同时提供有界、无正文的记录失败信号；本次未注入磁盘失败，不宣称已发生数据丢失 |
| R6 | P2 / 一致性风险 | [完整会话导出](../../src/lib/local-conversation-exporter.ts) 并行读取 head/revisions/runs，没有固定共同快照；运行日志又晚于 checkpoint 写入 | 明确导出的快照语义，使用受控并发写入验证；当前只认定风险，未复现不一致导出 |
| R7 | P2 / 维护问题 | 两个导出器重复上下文枚举/分块读取；[日志查询](../../src/lib/local-agent-run-log.ts) 每次扫描全局 JSONL，一行损坏可中断查询 | 先复用读取逻辑并补兼容/损坏用例；索引和轮转须有规模证据后再做，不能声称目前已有性能瓶颈 |
| R8 | P2 / 效果假设 | 历史 tsc --noEmit 成功后，tsc -b 构建发现类型错误；一次 read_file 使用未知 offset/limit | 固定项目初态，优先核对 scripts/tsconfig 和实际 schema；不要直接归因为模型能力或扩宽参数接口 |
| R9 | P3 / 维护问题 | AgentLoop 混合协议收集/循环/中断整理，页面承载会话加载/流消费/恢复/导出，存储承载 revision/租约/context | 随后续实际修改小步拆分，优先纯转换与重复逻辑；本次没有发现仅凭行数即可证明的功能错误 |

## 自动化验证与剩余人工验收

本次执行以下定向命令，18/18 通过，无失败或跳过：

```bash
npm test -- src/lib/local-agent-run-exporter.test.ts src/lib/local-agent-run-log.test.ts src/lib/local-conversation-store.test.ts src/web/conversation-operation-guard.test.ts src/web/chat-handler.test.ts tests/web-conversation-persistence.e2e.test.ts
```

| 能力 | 已有本次自动化证据 | 尚缺的验收 |
| --- | --- | --- |
| 导出 | 指定 runId 的前后检查点、上下文对象、私有文件权限；实际历史导出成功 | 活动运行中完整会话导出的快照一致性、更多损坏/缺失边界 |
| 并发保护 | CAS、跨实例写租约、活动读取、操作守卫测试 | 浏览器双标签真实运行时修改冲突，原任务仍能正常完成 |
| 恢复与保存 | 重建运行时继续完整协议；遗留标记恢复；保存失败 SSE | 浏览器中断后部分进度恢复与后续副作用行为 |
| 命令与进程 | 有现成进程测试文件，本次定向命令未包含它 | 占用端口反例、任务结束后的预览生命周期、取消后的进程/端口回收 |
| Agent 效果 | 一个历史开发样本的取证诊断 | 固定初态、多次真实运行与独立任务验收，不由单条日志推导成功率 |

前一轮全量 314/314 和 lint/typecheck/build 通过是本次对话先前执行结果；本次只报告上述定向检查，不把历史检查写成刚刚重跑。原 Specs 的人工验收复选框保持原状。

## 首批评测用例定义

复用 [历史 E1–E6](structured-system-prompt.md) 的任务意图，下面仅确定复测协议，不执行真实模型、不创建新评测程序。

所有用例分别从同一受控初态开始：`account.ts` 定义 UserAccount，`settings.ts` 导出 timeoutMs=1000，README 描述夹具。正式复测前固定文件完整字节及摘要；历史文档只有初态描述，不能冒充精确快照。记录任务原文、模型/配置、代码/Prompt 版本、独立会话、runId 和验收结果。

| 用例 | 独立验收标准 | 观察指标 |
| --- | --- | --- |
| E1 搜索 | 返回 UserAccount 的实际文件/行号；文件内容未变 | 搜索调用、无关探索、耗时 |
| E2 精确编辑 | 唯一变化为 timeoutMs 1000→1500；其他字节不变 | 是否先读、参数失败、写后验证 |
| E3 Plan | 文件快照不变、无副作用工具执行、计划含验证步骤 | 计划相关性、探索轮数 |
| E4 增量配置 | 保留 timeoutMs=1000 并新增 maxRetries=3，语法/编译验证通过 | 编辑次数、实际验证证据 |
| E5 失败恢复 | missing-settings 不存在；找到真实 settings 并正确报告 1000；文件未变 | 失败后恢复路径、额外读取 |
| E6 总结 | 不超过四行、结论与三个文件一致、文件未变 | 内容真实性、工具选择 |

另把本次看板任务保留为综合场景：构建通过之外，分别操作新增、跨列拖拽、刷新恢复、删除后再次刷新；记录最终预览是否应持续可访问。交互验收与构建验收分开报告。

每次优化只改变一个主要因素，固定条件多次复跑，同时比较任务通过情况、人工干预和消耗。先处理 R1–R3，再决定日志字段和维护重构；本轮没有将这些待办标为已完成。
